{% extends 'user/usermenu.html.twig' %}

{% block extracss %}


{% endblock %}

{% set tab_active = 'main' %}

{% block body_id 'user_edit_profile' %}

{% block main %}
    <h1>{{ 'title.edit_user_main'|trans }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">
            <i class="fa fa-save" aria-hidden="true"></i> {{ 'action.save'|trans }}
        </button>
    {{ form_end(form) }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}


{% endblock %}
